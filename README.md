# Anaamath
Anaamath is an application that uses web sockets to create a two way messaging interface that connects two users in a anonymous way, and clears the browser history whenever that user closes the browser. Each message would be stored and cached inside the browsers sqlite db
